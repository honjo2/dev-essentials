# CLAUDE.md

このファイルは、AI コーディングエージェント向けのリポジトリ全体の指示を記載しています。

変更を加える前にこのファイルを読んでください。タスクがドメインモデリング、命名、アーキテクチャ、UI、または長期的に有効なプロダクト挙動に関わる場合は、`CONTEXT.md`、`DESIGN.md`、および関連する ADR も読んでください。

## 基本原則

- 小さく焦点の絞られた変更を優先する。
- 新しいパターンを導入する前に、既存のコードパターンに従う。
- 明示的に必要でない限り、大きなドキュメントファイルを作成しない。
- 新しいドキュメントを作成するより、既存ファイルの編集を優先する。
- 有用なプロジェクト固有の内容を保持せずに、既存ドキュメントを上書きしない。
- 長期的に有効な知識は適切な場所に置く:
  - ドメイン言語 → `CONTEXT.md`
  - デザイン原則 → `DESIGN.md`
  - 意思決定とトレードオフ → `docs/adr/`
  - 運用手順 → `docs/runbook.md`
  - 設計・実装の作業記録 → `docs/superpowers/`
  - レビュー用 HTML 成果物 → `docs/pages/`

## スキル適用ルール

Superpowers スキルは汎用の開発プロセスを定めています。タスクに適用する可能性があれば、必ず該当する Superpowers スキルを呼び出してください。

優先順位:

1. ユーザーからの明示的な指示（`CLAUDE.md` / `GEMINI.md` / `AGENTS.md`、および直接の依頼）
2. Superpowers スキル
3. デフォルトのシステムプロンプト

主な Superpowers スキル:

- `brainstorming` — 機能追加・UI 変更・挙動変更の前に設計を対話で固める
- `writing-plans` — 複数ステップの実装計画を作成する
- `subagent-driven-development` / `executing-plans` — 計画をタスク単位で実行する
- `test-driven-development` — 機能実装・バグ修正の前に失敗するテストを書く
- `systematic-debugging` — バグやテスト失敗の根本原因を調査する
- `verification-before-completion` — 完了宣言の前に検証コマンドを実行する
- `finishing-a-development-branch` — 作業完了後に統合方法を決定する

## 実装前

新しい機能、UI 変更、挙動変更を始める前は、`brainstorming` スキルで設計を対話により固め、承認を得てください。スコープが小さく自明な場合のみ省略できます。

自明でない変更の場合:

1. 関連するソースコードとテストを確認する。
2. 命名とドメインルールについて `CONTEXT.md` を読む。
3. UI 変更の場合は `DESIGN.md` を読む。
4. `docs/adr/` 配下の関連する ADR を読む。
5. 複数ステップの実装が必要な場合は、`writing-plans` スキルで計画を作成する。

## 実装中

- `CONTEXT.md` で定義されたユビキタス言語を使用する。
- 文書化されていない限り、ドメイン概念の同義語を導入しない。
- 使い捨てのコンポーネントを作るより、既存コンポーネントの更新を優先する。
- 生成コードは読みやすく、周辺コードと一貫した状態に保つ。
- 機能開発に広範なリファクタリングを混ぜない。
- セキュリティ、認証、認可、テナンシー、データ保持、プライバシーに関する挙動を黙って変更しない。
- 機能実装・バグ修正には `test-driven-development` スキルを適用する。
- バグやテスト失敗には `systematic-debugging` スキルで根本原因を調査してから修正する。

## 検証

作業を完了とみなす前に、`verification-before-completion` スキルを適用し、プロジェクト固有のチェックを実行してください。主張の前に必ず新鮮な検証証拠を得てください。

このセクションは使用するスタックに合わせて更新してください:

```bash
# pnpm test
# pnpm typecheck
# pnpm lint
```

チェックを実行できない場合は、その理由と代わりに何を検証したかを明記してください。

## ドキュメント更新ルール

長期的に有効な知識を残す場合にのみ、ドキュメントを更新してください。意思決定が長期的に有効かどうか不確かな場合は、一時ドキュメントを膨らませるのではなく ADR を提案してください。

変更が重要な意思決定の導入または撤回を伴う場合は、ADR を追加または更新してください。特に以下に関わる場合:

- アーキテクチャ
- 外部サービスへの依存
- 認証 / 認可
- テナンシー境界
- データの保存・保持
- バックグラウンド実行
- オブザーバビリティ
- AI モデル / プロバイダ / ツールの選定

以下の場合は `CONTEXT.md` を更新してください:

- 新しいドメイン概念が導入されたとき
- 用語の名前が変更されたとき
- 2 つの概念が混同されやすいとき
- 命名がスキーマ、API、UI、ビジネスロジックに影響するとき

以下の場合は `DESIGN.md` を更新してください:

- 新しい再利用可能な UI パターンが導入されたとき
- ビジュアル言語が変わったとき
- アクセシビリティ、レイアウト、インタラクションのルールが変わったとき

長期的に有効な意思決定を含まない限り、一時的な実装計画を残さないでください。有効な部分は ADR、`CONTEXT.md`、`DESIGN.md` に要約してください。

## Superpowers ワークフロー

`docs/superpowers/plans/` と `docs/superpowers/specs/` は、設計・実装過程で作成する作業資料です。Superpowers スキルはこれらを保存・コミットし、実装時の参照先として使います。

- `docs/superpowers/specs/` — `brainstorming` スキルで承認された設計・仕様を `YYYY-MM-DD-<topic>-design.md` として保存する
- `docs/superpowers/plans/` — `writing-plans` スキルで作成した実装計画を `YYYY-MM-DD-<feature-name>.md` として保存する
- これらのファイルは実装時の source of truth として機能し、完了後もプロジェクトの履歴として保持する
- 長期的に有効な知識は、恒久的なドキュメントへ昇格させる:
  - 設計上の意思決定 → `docs/adr/`
  - ドメイン言語 → `CONTEXT.md`
  - UI の原則 → `DESIGN.md`
  - 運用手順 → `docs/runbook.md`
- 古くなった plans/specs を整理したい場合は `docs/superpowers/archive/` などを任意で使う

## HTML レビュー成果物

AI エージェントは、人間によるレビューのために `docs/pages/` 配下に一時的な HTML 成果物を作成できます。

HTML 成果物の用途:

- UI プロトタイプ
- アーキテクチャ図
- 比較レポート
- デザイン検討
- ステークホルダーレビュー用ページ

成果物は `docs/pages/` 配下の `reports/`、`prototypes/`、`diagrams/` に日付プレフィックス付き（`YYYY-MM-DD-topic.html`）で置いてください。成果物を追加・削除した場合は、`docs/pages/index.html` の該当セクションのリンクも更新してください。

HTML 成果物は信頼できる情報源（source of truth）ではありません。意思決定の後は、長期的に有効な知識を ADR、`CONTEXT.md`、`DESIGN.md` に記録してください。
