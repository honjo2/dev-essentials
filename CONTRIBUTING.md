# Contributing

コントリビューションありがとうございます。

このリポジトリは、軽量で長期的に有効なドキュメントと、人間が責任を持つ AI 支援開発を重視しています。

## 開発フロー

1. Issue / Discussion を作成するか、既存のものを選ぶ。
2. 新しい機能・UI 変更・挙動変更の場合は、`brainstorming` スキルで設計を対話により固め、承認を得る。
3. 複数ステップの実装が必要な場合は、`writing-plans` スキルで計画を作成する。
4. フィーチャーブランチで変更を実装する。
5. 必要に応じてテストを追加・更新する。
6. プルリクエストを作成する。

## ブランチ命名

短く説明的なブランチ名を使用してください:

- `feature/...`
- `fix/...`
- `refactor/...`
- `docs/...`

## PR 作成前

以下を確認してください:

- テストが通る
- 型チェックが通る
- lint が通る
- 必要に応じて関連ドキュメントが更新されている
- 重要な設計上の意思決定が ADR として記録されている
- 新しいドメイン用語が `CONTEXT.md` に追加されている
- UI パターンの変更が `DESIGN.md` に反映されている

## AI 支援開発

AI エージェントは、実装、リファクタリング、テスト、ドキュメント更新、レポート、プロトタイプに使用できます。

作業では Superpowers スキルを適切に適用してください:

- 設計・仕様の検討には `brainstorming`
- 実装計画には `writing-plans`
- 計画実行には `subagent-driven-development` または `executing-plans`
- 機能実装・バグ修正には `test-driven-development`
- バグ調査には `systematic-debugging`
- 完了宣言前には `verification-before-completion`
- ブランチ完了時には `finishing-a-development-branch`

PR を提出する前に、AI が生成したすべての変更をレビューする責任はコントリビューターにあります。

以下を確認せずに AI が生成した変更を受け入れないでください:

- 正しさ
- セキュリティへの影響
- 既存アーキテクチャとの一貫性
- `CONTEXT.md` との一貫性
- UI に影響する場合は `DESIGN.md` との一貫性
- ADR が必要かどうか
- 検証コマンドが通っているかどうか

## ドキュメントポリシー

すべての実装詳細について詳細なドキュメントを作成しないでください。

以下を優先してください:

- ユビキタス言語とドメインコンテキストは `CONTEXT.md`
- UI の原則とビジュアルアイデンティティは `DESIGN.md`
- 重要な意思決定は `docs/adr/`
- 運用手順は `docs/runbook.md`
- 一時的な人間レビュー用 HTML 成果物は `docs/pages/`
- 設計・実装過程の作業記録は `docs/superpowers/`

大きなタスクの完了後は、長期的に有効な知識を恒久的なドキュメントへ昇格させます。`docs/superpowers/` の plans/specs は実装時の参照先・履歴として保持し、削除は任意とします。

## Issue / Discussion / ADR の使い分け

- 実行可能な作業には Issue を使用する。
- オープンエンドなフィードバック、アイデア、プロダクトに関する質問には Discussion を使用する。
- アーキテクチャ、運用、セキュリティ、データ、プロダクトの方向性に影響する、承認済みまたは意図的に保留された意思決定には ADR を使用する。
